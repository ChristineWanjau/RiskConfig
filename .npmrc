# NPM configuration for Azure App Service deployment
registry=https://registry.npmjs.org/
always-auth=false
engine-strict=true
fund=false
audit=false
progress=false
loglevel=warn

# Optional: Uncomment if you're using private packages
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Azure App Service specific settings